# 자율주행 경로 설명 및 계획 Agent 프로젝트 보고서

- **학번:** 20252416
- **이름:** 조성관

## 1.1 기본정보

### 프로젝트명
**AI 기반 자율주행 경로 계획 및 주행 전략 설명 Agent**

### 참고 프로젝트 링크
- [nuPlan – A closed-loop ML-based planning benchmark for autonomous driving](https://www.nuscenes.org/nuplan)
- [Waymo Open Dataset](https://waymo.com/open/)
- [CARLA – Open-source simulator for autonomous driving research](https://carla.org/)
- [Large Language Model Enhanced Differentiable Trajectory Planning](https://arxiv.org/abs/2607.10438)

### 대상 도메인
**자율주행(Autonomous Driving) / 지능형 교통 시스템(ITS)**

세부적으로는 자율주행 차량의 **주행 환경 분석, 경로 계획(Path Planning), 주행 전략 수립 및 자연어 기반 설명**을 대상으로 한다.

---

## 1.2 문제 정의

### 문제 정의
자율주행 차량은 주변 차량, 보행자, 차선, 교통신호, 도로 구조 및 목적지 등의 정보를 종합적으로 분석하여 안전하고 효율적인 주행 경로를 결정해야 한다. 기존의 자율주행 시스템은 주행 경로와 행동을 계산하는 데 초점을 두는 경우가 많아, **왜 특정 경로와 행동을 선택했는지 사용자가 이해하기 어려운 문제**가 있다.

본 프로젝트에서는 Vision Model 및 주행 데이터를 통해 주변 환경을 파악하고, LLM 기반 Agent가 해당 정보를 종합하여 목적지까지의 경로 후보를 분석한다. 이후 거리, 예상 시간, 주변 위험요소, 차선 변경 필요성 등의 조건을 고려하여 주행 전략을 수립하고, 그 판단 근거를 자연어로 설명하는 시스템을 구축한다.

### 목표
1. 주변 차량, 보행자, 차선, 신호등 등의 주행 환경 정보를 구조화한다.
2. 목적지까지 이동 가능한 경로 후보를 생성하고 비교한다.
3. 안전성, 거리, 예상 시간 및 주행 복잡도 등을 고려하여 주행 전략을 수립한다.
4. LLM Agent를 이용하여 선택된 경로와 행동의 이유를 자연어로 설명한다.
5. 사용자가 이해하기 쉬운 형태로 최종 경로와 구간별 주행 계획을 제공한다.

---

# 2.1 모델 및 프레임워크

### 기반 LLM 모델
**OpenAI GPT 계열 모델**

주행 환경에서 추출된 구조화 정보를 입력으로 받아 상황을 해석하고, 경로 후보를 비교하며, 구간별 주행 전략과 자연어 설명을 생성하는 데 활용한다.

### 에이전트 프레임워크
**LangChain 기반 Agent**

LangChain을 이용하여 LLM이 필요한 외부 도구를 선택하고 호출할 수 있도록 구성한다. 예를 들어 지도/경로 API, 환경 분석 모듈, 경로 계산 모듈 등의 도구를 Agent가 상황에 따라 호출하도록 설계한다.

---

# 2.2 연계 기술 및 도구

### 적용 기술

- **Computer Vision:** 주행 이미지 또는 영상에서 차량, 보행자, 차선, 신호등 등의 객체 및 환경 정보 추출
- **Object Detection:** YOLO 계열 모델 등을 이용한 주변 객체 탐지
- **LLM:** 주행 상황에 대한 의미 이해, 경로 후보 비교, 주행 전략 생성 및 자연어 설명
- **Agent Workflow:** 환경 분석 → 도구 호출 → 경로 비교 → 계획 수립 → 결과 설명의 순차적 작업 수행
- **Path Planning:** 목적지까지의 경로 후보 생성 및 거리·시간·위험요소 비교
- **RAG/외부 지식 활용:** 필요한 경우 교통 규칙 및 주행 관련 정보를 검색하여 Agent의 판단에 활용
- **Web UI:** Streamlit을 이용한 입력 및 분석 결과 시각화

### 외부 도구 및 연동 API

- **Google Maps Platform / Directions API:** 출발지와 목적지 사이의 경로 및 도로 정보를 제공하는 용도로 활용 가능
- **OpenStreetMap / OSRM:** 공개 지도 데이터와 경로 탐색 기능을 활용하는 대안
- **CARLA:** 실제 차량 대신 가상 환경에서 차량, 보행자, 교통신호 및 도로 상황을 구성하고 주행 계획을 검증하는 시뮬레이션 도구
- **YOLO:** 이미지 또는 영상에서 주변 객체를 탐지하는 Computer Vision 도구
- **Streamlit:** Agent의 입력 화면, 지도/경로 정보 및 분석 결과를 웹 인터페이스로 제공

※ 실제 구현 단계에서는 API 사용 가능 여부, 호출 비용, 라이선스 및 데이터 이용 조건을 확인하여 최종 도구를 선정한다.

---

# 3.1 입력 및 데이터

### 사용자 입력 데이터 형태

사용자는 다음과 같은 형태의 정보를 입력한다.

1. **출발지**
   - 현재 위치 또는 사용자가 지정한 출발 위치
2. **목적지**
   - 주소, 장소명 또는 좌표
3. **주행 조건**
   - 안전 우선, 빠른 이동 등 사용자가 선택할 수 있는 주행 목적
4. **주행 환경 데이터**
   - 카메라 이미지 또는 주행 영상
   - 차량, 보행자, 신호등, 차선 등의 객체 정보
5. **외부 데이터**
   - 지도 및 도로 네트워크
   - 경로 정보
   - 필요한 경우 교통 및 도로 관련 정보

### 입력 예시

```text
출발지: 서울역
목적지: 강남역
주행 목적: 안전성 우선
환경 정보:
- 전방 차량 2대
- 우측 차선 차량 1대
- 전방 교차로 존재
- 현재 신호: 녹색
- 200m 전방 우회전 필요
```

---

# 3.2 동작 단계

### Step 1. 사용자 입력 수집
출발지, 목적지 및 주행 조건을 입력받는다.

### Step 2. 주행 환경 분석
Computer Vision 모델 또는 제공된 환경 데이터를 이용하여 차량, 보행자, 차선, 신호등 및 도로 구조를 파악한다.

### Step 3. 외부 도구 호출
Agent가 필요한 경우 지도 및 경로 API를 호출하여 목적지까지의 경로 후보와 도로 정보를 수집한다.

### Step 4. 경로 후보 생성
수집된 정보를 바탕으로 목적지까지 이동할 수 있는 여러 경로 후보를 구성한다.

### Step 5. 경로 후보 분석
각 후보에 대해 이동 거리, 예상 시간, 차선 변경 횟수, 교차로 및 위험요소 등을 비교한다.

### Step 6. 주행 전략 수립
LLM Agent가 현재 환경과 경로 정보를 종합하여 구간별 주행 행동을 결정한다.

예:
**직진 → 차선 유지 → 우측 차선 변경 → 감속 → 우회전 → 직진**

### Step 7. 판단 근거 생성
Agent가 선택한 경로와 주행 전략을 선택한 이유를 자연어로 생성한다.

### Step 8. 결과 출력
최종 경로, 주행 단계, 위험요소 및 판단 근거를 사용자에게 제공한다.

---

# 3.3 최종 출력

최종 결과는 **경로 정보 + 주행 전략 + 자연어 설명**의 형태로 반환한다.

### ① 경로 정보
- 추천 경로
- 총 거리
- 예상 소요 시간
- 주요 경유 구간

### ② 구간별 주행 계획
```text
1. 현재 차선 유지
2. 약 100m 전방에서 우측 차선으로 이동
3. 교차로 진입 전 감속
4. 우회전
5. 목적지까지 직진
```

### ③ 상황 및 위험요소
```text
전방 교차로에서 우회전이 필요합니다.
우측 차선에 차량이 존재하므로 즉시 차선 변경하지 않고
안전거리를 확보한 후 차선 변경하도록 계획했습니다.
```

### ④ 최종 Agent 응답 예시

> **주행 계획:** 현재 차선을 유지한 후 약 100m 전방에서 우측 차선으로 이동하고, 교차로 진입 전 감속하여 우회전합니다.  
>
> **판단 근거:** 우회전을 위해 우측 차선 진입이 필요하지만 현재 우측 차선에 차량이 있어 즉시 차선 변경하기보다 안전거리를 확보한 후 이동하는 전략을 선택했습니다.  
>
> **주의사항:** 교차로 진입 시 신호 및 보행자 유무를 확인해야 합니다.

---

# 4.1 적용 가능성 검토

## 참고 프로젝트의 한계

### nuPlan
nuPlan은 자율주행 Planning을 평가하기 위한 대표적인 데이터셋 및 벤치마크이지만, 실제 프로젝트에서 이를 그대로 활용하기에는 데이터와 평가 환경의 복잡성이 높을 수 있다. 또한 실제 주행 상황의 모든 예외적인 상황을 데이터만으로 표현하기 어렵다는 한계가 있다.

### Waymo Open Dataset
Waymo Open Dataset은 대규모 자율주행 데이터를 제공하지만, 데이터 규모와 구조가 복잡하여 소규모 프로젝트에서 전체 데이터를 직접 활용하기에는 전처리 및 시스템 구축 부담이 크다.

### CARLA
CARLA는 다양한 자율주행 상황을 시뮬레이션할 수 있지만, 시뮬레이션 환경과 실제 도로 환경 사이에는 차이가 존재한다. 따라서 CARLA에서 정상적으로 동작하는 계획이 실제 도로에서도 동일하게 동작한다고 보장할 수 없다.

### LLM 기반 자율주행 Planning 연구
LLM은 복잡한 주행 상황을 자연어로 해석하고 설명하는 데 활용할 수 있지만, LLM의 생성 결과가 항상 정확하거나 일관된 것은 아니다. 특히 실제 차량의 안전과 직접 연결되는 저수준 제어를 LLM에 직접 맡기는 것은 적절하지 않으며, 환각(Hallucination)이나 잘못된 판단 가능성을 고려해야 한다.

## 내 프로젝트 적용점

본 프로젝트에서는 참고 프로젝트의 규모를 그대로 구현하기보다는 **고수준 주행 계획 및 설명 Agent**에 초점을 맞춘다.

- nuPlan의 **Planning 및 경로 평가 개념**을 참고한다.
- Waymo Open Dataset의 **주행환경 및 객체 정보 활용 방식**을 참고한다.
- CARLA의 **가상 주행환경 및 시뮬레이션 방식**을 활용할 수 있다.
- LLM Planning 연구의 **고수준 상황 이해 및 주행 전략 생성 방식**을 참고한다.
- 최종적으로 LLM Agent가 환경 정보를 입력받아 경로 후보를 비교하고, 구간별 주행 전략과 판단 근거를 자연어로 설명하도록 구현한다.

## 개선 방안

1. **LLM 단독 판단을 제한**
   - 경로 계산과 객체 탐지는 전문 알고리즘 및 별도의 모델이 담당하고, LLM은 고수준 분석과 설명을 담당하도록 역할을 분리한다.

2. **외부 도구 기반 검증**
   - LLM이 생성한 경로를 지도 API 또는 경로 탐색 알고리즘으로 검증하여 존재하지 않는 도로 및 잘못된 경로 생성을 줄인다.

3. **안전 규칙 기반 검증**
   - 신호 위반, 보행자 충돌 위험, 과도한 차선 변경 등 안전 관련 조건은 별도의 규칙 기반 모듈을 통해 검증한다.

4. **시뮬레이션 기반 평가**
   - CARLA 등의 시뮬레이션 환경에서 다양한 교차로, 차선 변경, 보행자 출현 등의 상황을 구성하여 Agent의 계획을 평가한다.

5. **설명 가능성 강화**
   - 단순히 최종 경로만 제시하지 않고, 경로 선택 이유와 주요 위험요소를 함께 제공하여 사용자가 Agent의 판단을 이해할 수 있도록 한다.

---

## 프로젝트 기대효과

본 프로젝트는 Computer Vision을 통한 환경 인식과 LLM Agent를 통한 상황 이해 및 주행 전략 생성을 결합하여, 기존의 단순 경로 탐색 시스템보다 **설명 가능한 자율주행 경로 계획 시스템**을 구현하는 것을 목표로 한다.

이를 통해 사용자는 자율주행 Agent가 선택한 경로뿐만 아니라 **어떤 환경 정보를 바탕으로 어떤 주행 전략을 선택했는지**를 자연어로 확인할 수 있다.
